diff --git a/node_modules/karma-typescript/dist/bundler/dependency-walker.js b/node_modules/karma-typescript/dist/bundler/dependency-walker.js
index aa4ae53..c6ab44a 100644
--- a/node_modules/karma-typescript/dist/bundler/dependency-walker.js
+++ b/node_modules/karma-typescript/dist/bundler/dependency-walker.js
@@ -49,6 +49,15 @@ var DependencyWalker = /** @class */ (function () {
                     });
                 }
             }
+            else if (!resolvedModules && !queued.emitOutput.isDeclarationFile) {
+                // Typescript 6+: sourceFile.resolvedModules was removed; fall back to sourceFile.imports
+                var imports = queued.emitOutput.sourceFile.imports;
+                if (imports) {
+                    imports.forEach(function (importNode) {
+                        _this.addBundleItem(queued, undefined, importNode.text, ambientModuleNames);
+                    });
+                }
+            }
             dependencyCount += queued.item.dependencies.length;
         });
         this.validateCase(queue);
